# 行业研究 Agent 界面改造 Brief

## 设计方向

面向求职作品集和真实研究任务的“冷静、可信、证据驱动的研究工作台”。避免常见 AI 聊天页的紫色渐变、大面积气泡和静态六维进度条。

用户打开页面后首先理解三件事：当前 Agent 在做什么、为什么要问这个问题、已有结论由哪些证据支持。

## 页面布局

### 左侧：研究会话

- 新建研究；
- 历史会话；
- 当前场景和研究目标；
- 研究阶段：理解需求、访谈、研究、待确认、报告完成。

不再一直展示固定六维评分。只有研究计划确实包含某维度时才显示对应进度。

### 中间：对话与研究过程

- 用户消息；
- 高价值追问卡片，显示“为什么要问”；
- 快捷选项和“按默认假设继续”；
- 研究计划卡片；
- 阶段性结论；
- 报告确认按钮；
- 正式报告预览。

工具调用和内部 Supervisor 决策默认折叠，不刷屏。

### 右侧：证据抽屉

- Evidence 数量；
- 来源标题、机构、发布日期和地区；
- 原文摘录；
- 打开来源；
- 来源类型和置信度；
- 冲突或过期提示。

小屏幕时右侧变为底部抽屉。

## 核心交互状态

```text
欢迎页
→ 场景识别
→ 自适应访谈
→ 研究计划确认
→ 联网研究进度
→ 阶段结论
→ 继续提问 / 生成报告
→ 报告预览和下载
```

## 功能优先级

P0：场景标签、追问选项、研究计划、证据抽屉、报告确认、下载按钮、Follow-up。

P1：历史会话、记忆管理、搜索预算和耗时显示、移动端适配。

P2：PDF／产品目录／报价单入口，仅在相关场景显示。

## 视觉规范

- 主色使用深蓝灰或墨绿色，体现研究工具的可信感；
- 强调色只用于当前动作和关键状态；
- 使用一套 4/8px 间距系统；
- 卡片圆角克制，减少大面积阴影；
- 正文优先中文可读性和长报告阅读；
- 动画只用于节点状态、抽屉和消息进入；
- 支持 `prefers-reduced-motion`；
- 保证键盘操作、焦点状态和可读对比度。

## 不建议做

- 不为了显得复杂而展示所有 Agent 的内部思考；
- 不把工具日志当作主内容；
- 不始终展示六个固定维度；
- 不使用大量渐变、玻璃效果和无意义动画；
- 不在普通问题中显示上传文件；
- 不把 Markdown 原文直接显示给用户。

## 实现建议

第一版继续使用现有 Starlette + HTML/CSS/JavaScript，先重构组件和状态，不必为了好看立即迁移 React。浏览器验证覆盖 1440px、1024px 和 390px 三种宽度。

