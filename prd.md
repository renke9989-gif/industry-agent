# 行业研究与决策访谈 Agent PRD

版本：V6.0
状态：产品范围冻结，进入实现

## 1. 产品定位

这是一个先理解用户、再联网研究、最后辅助决策的行业研究 Agent。它不是“用户说一句话就套一份行业报告”的报告生成器。

系统必须根据用户的实际问题，在以下行为中选择一种：

- 直接回答具体问题；
- 追问一个真正阻塞决策的信息；
- 生成并执行针对性的研究计划；
- 诊断已运营项目；
- 比较多个方案；
- 生成正式报告；
- 在报告前后回答窄范围追问。

行业事实、政策、价格、公司产品和市场状态通过实时联网获得。用户记忆和会话记忆不能替代最新搜索。

## 2. 核心用户场景

### 2.1 构思阶段

用户只有想法，尚未实施。重点了解目标地区、预算区间、个人资源和最大风险；使用行业基准和保守／中性／乐观情景，不要求不存在的真实经营数据。

### 2.2 筹备阶段

用户正在选址、选产品、组建团队或比较方案。重点研究候选方案、竞争、成本、预算优先级和落地顺序。

### 2.3 已运营项目诊断

用户已有公司、工厂、门店或产品，希望解决销量下降、成本上升、产能不足或客户流失。围绕具体问题建立假设、补采数据并设计验证实验，不输出创业模板。

### 2.4 现有能力扩展

用户已有产品、工厂、设备或软件，准备增加设备、功能、产线或服务。先梳理现有能力和目标能力差距，再比较保留现有能力、局部改造和重新建设。

### 2.5 企业／公司研究

用户研究公司、业务线、产品、芯片、设备、客户、供应链或竞争位置。输出公司—业务线—产品／设备—客户／供应链关系，不套开店、客单价或 ROI 模板。

### 2.6 市场问题与事实查询

用户只询问市场规模、政策、趋势、城市推荐或一个具体事实。优先直接回答；只有地区、年份或口径会改变答案时才追问。

### 2.7 方案比较

用户在行业、城市、供应商或技术路线之间选择。统一口径比较候选项、决策权重、成本、时间和不可接受风险。

## 3. 防止模板化回答的强制路由

收到用户消息后，先进行 `Request Triage`，输出结构化字段：

```text
scenario
decision_goal
answer_type
complexity
needs_web
needs_interview
needs_full_report
blocking_unknowns
```

`answer_type` 只能取：

```text
direct_answer
clarifying_question
research_plan
recommendation
diagnosis
comparison
insufficient_evidence
report_confirmation
report
```

路由规则：

1. 具体事实问题进入 `direct_answer`，必要时执行窄范围搜索，不进入完整报告。
2. 报告后的追问进入 `followup`，复用 Evidence，只回答当前问题。
3. 只有用户明确要求调研、评估、比较或报告时，才执行完整研究计划。
4. 只有一个未知信息会显著改变建议时，才进入 `clarifying_question`。
5. 证据不足时必须同时给出已知事实、缺口、暂时建议和最低成本验证动作。
6. 报告结构由场景和决策目标生成，不允许强制所有任务输出固定六维模板。

验收时重点检查：用户的问题是否在回答第一段得到直接回应。如果没有直接回应，则该回答失败，即使包含很多行业信息。

## 4. 自适应访谈

访谈轮数不固定。每轮结束后计算：

```text
goal_clarity
scenario_clarity
evidence_readiness
blocking_unknowns
question_information_value
```

深度调研采用问卷式访谈。开店构思和筹备场景通常五至六题，经营诊断约六题，复杂工厂改造最多八题；简单事实问题仍然可以零轮直接回答。

继续追问必须满足：存在会改变研究方向或建议结论的关键未知信息。否则使用公开基准或明确的保守假设继续。

每次只展示一个问题。问卷卡片必须包含访谈题号、信息准备度、问题主题、为什么要问、三至五个可点击答案、自定义输入、“暂不确定”和“结束访谈，开始调研”。

访谈回答进入结构化 `known_facts`，已经回答的主题不得重复询问。界面持续显示已确认信息和准备度，让用户明确感知系统正在建立专属决策背景，而不是收集通用报告字段。

系统使用最大访谈轮数、访谈时间预算和连续无新增信息次数防止无限追问。用户可以随时要求直接开始。

## 5. 动态研究流程

```text
用户问题
  → Request Triage
  ├─ Direct Answer：窄范围搜索 → 直接回答
  ├─ Clarify：高价值追问 → 更新用户资料 → 重新判断
  └─ Research：生成 Research Plan
        → Search
        → Fetch Page
        → Extract Evidence
        → Verify Evidence
        → 阶段结论
        → Report Confirmation
```

研究计划必须包含目标、子问题、证据需求、来源优先级、工具预算、停止条件和输出类型。

权威一手来源可以单独支持结论；一般关键数字优先使用两个独立来源。找不到可靠数据时输出区间或证据不足，不补造精确值。

## 6. Evidence 与报告

Evidence 至少包含：事实或主张、数值、单位、时期、地区、来源标题、URL、发布者、来源类型、发布时间、检索时间、原文摘录、数据类型、是否一手来源和置信度。

所有关键外部数字必须绑定 Evidence ID。引用 ID 必须存在，URL 必须有效。转载来源不能重复计算为独立证据。

正式报告使用普通中文排版，不向用户显示 Markdown 标记、原始 JSON 或代码块。

报告先回答用户的决策问题，再展示背景、假设、关键发现、建议、情景测算、风险、证据缺口和来源。与当前场景无关的章节不生成。

## 7. 报告确认门

完整研究结束后先展示简短阶段结论，并询问：

“当前研究已经完成。请问您还想继续提问，还是现在生成正式报告？”

按钮：

```text
继续提问
生成报告
```

继续提问：保留 checkpoint 和 Evidence，进入 Follow-up，只回答当前问题。

生成报告：从当前 checkpoint 进入报告节点，生成后显示下载按钮。重复点击必须幂等。

简单事实查询不显示报告确认门；只有用户明确要求时才生成报告。

## 8. Memory

### 8.1 工作记忆

保存当前消息、Research Plan、Evidence、工具事件和未完成任务。

### 8.2 会话记忆

保存本次研究摘要、已确认背景、报告和追问，用于恢复会话。

### 8.3 用户记忆

只保存用户明确授权的稳定信息，例如公司、设备、长期偏好和约束。记录来源、有效期、置信度、状态和变更时间线，支持查看、更正和删除。

### 8.4 系统经验

保存人工审核后的失败案例、来源规则、场景路由和评测结论。模型输出不能自动升级为生产规则。

第一阶段使用 SQLite 结构化存储；生产版本使用 PostgreSQL。暂不使用向量数据库。

## 9. Tool / MCP

分析师只依赖统一 `ResearchProvider`。生产默认 MCP，失败时降级 Direct，测试使用 Fake Provider。

工具统一记录来源、耗时、成功状态、重试、错误码和结果数量。MCP 不可用不能导致整个应用无法启动。

## 10. 文档优先的多模态扩展

多模态不属于当前主链。未来优先支持：

1. 技术 PDF、产品目录和说明书；
2. 报价单、采购清单和财报表格；
3. 设备铭牌和参数截图；
4. 最后再考虑设备、产线和门店现场照片。

解析结果必须带文件 ID、文件名、页码或区域、解析方式和文件时间。视觉识别结果不能直接作为公开事实，关键字段需要用户确认或联网验证。

## 11. 数据和部署

第一阶段使用 SQLite 保存 sessions、checkpoints、memories、evidence、tool_events、reports 和 eval_runs。

生产版本使用 PostgreSQL；Redis 只用于缓存、限流和异步任务状态。搜索缓存设置 TTL，不作为行业知识库。

## 12. Harness 与评测

离线 Harness 验证路由、状态、工具降级、记忆隔离、引用契约和安全规则。

在线评测记录任务完成率、直接回答命中率、关键结论引用覆盖率、URL 有效率、证据支持率、时效性、工具成功率、延迟、Token、成本和重复运行稳定性。

必须增加模板化专项指标：

```text
direct_answer_accuracy
unnecessary_report_rate
unnecessary_question_rate
first_paragraph_goal_alignment
scenario_template_leakage_rate
```

场景测试至少覆盖：简单事实零轮访谈、一次性提供完整信息、用户跳过追问、构思开店、运营诊断、工厂扩展、企业研究、城市推荐、报告确认、报告后追问、记忆删除和用户隔离。

## 13. 实施顺序

P0：Request Triage、场景识别、自适应访谈、Direct Answer、动态 Research Plan、报告确认门、普通文本报告。

P1：SQLite checkpoint、会话摘要、用户授权长期记忆、记忆 Harness。

P2：在线评测完善、Single/Multi Agent 消融、认证、限流、Tracing、成本观测。

P3：PDF、产品目录、报价单、设备铭牌的解析和 Evidence 验证。

## 14. 当前版本边界

当前是作品集版本。生产化仍需认证、租户隔离、限流、任务取消、持久化 checkpoint、多 worker 状态共享、日志和数据治理。
